<Window x:Class="StockKLineApp.Views.StockDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="股票详情" Height="640" Width="980"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="股票详情" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
        <TextBlock Grid.Row="1" Text="{Binding CodeLabel}" Margin="0,0,0,8" FontSize="14" />
        <TextBlock Grid.Row="2" Text="{Binding NameLabel}" Margin="0,0,0,8" FontSize="14" />
        <TextBlock Grid.Row="3" Text="{Binding SymbolLabel}" Margin="0,0,0,8" FontSize="14" />
        <ScottPlot:WpfPlot Grid.Row="4" x:Name="CandlestickPlot" />
    </Grid>
</Window>
